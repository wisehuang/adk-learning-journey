"""Entry point for running the multi-agent project management system as a module."""

# Use relative import if we're in the package
from .main import run_system

if __name__ == "__main__":
    run_system() 